{
  "name": "video-text-render",
  "version": "1.0.0",
  "description": "Arabic text video generator",
  "main": "server.py",
  "scripts": {
    "start": "python server.py"
  },
  "dependencies": {
    "puppeteer": "^21.6.0"
  },
  "puppeteer": {
    "args": [
      "--no-sandbox",
      "--disable-setuid-sandbox",
      "--disable-dev-shm-usage",
      "--single-process",
      "--no-zygote"
    ]
  }
}
